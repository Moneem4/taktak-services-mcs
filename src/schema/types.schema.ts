export enum PassType {
    CUSTOM = "CUSTOM",
    GUEST = "GUEST",
    BLACK_LISTED = "BLACK_LISTED",
  
}


export enum Visibility {
	PRIVATE="PRIVATE",
	PUBLIC="PUBLIC"
}

 export  enum State {
    DONE="DONE",
	PENDING="PENDING"
}

export enum ServiceType{
 GUEST=	"FREE",
 PREMIUM="PREMIUM"
}
export enum Access {
    PRIVATE="PRIVATE",
	PUBLIC="PUBLIC",
	BLACK_LISTED="BLACK_LISTED"
}